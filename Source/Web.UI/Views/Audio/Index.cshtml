@model Ewk.BandWebsite.Web.UI.Models.ItemListModel<Ewk.BandWebsite.Web.UI.Models.AudioAdapterSettings.AudioDetailsModel>

@{
    ViewBag.Title = "Music";
}

<div id="music" class="entityview">
    
    <div class="leftColumn">
        <div class="summary">
            <div class="title">
                @Model.Title
                @if(Request.IsAuthenticated)
                {
                    <span class="editLink">
                        @Html.ActionLink("audio settings", "Index", "AudioAdapterSettings")
                    </span>
                }
            </div>
            <div class="items">
                @foreach (var item in Model.Items)
                {
                    @Html.Partial("_Item", item)
                }
            </div>
        </div>
    </div>

    <div class="rightColumn">
        <div class="summary">
            <div class="title">
                Albums
            </div>
            <div class="items">
                Coming soon...
            </div>
        </div>
    </div>

</div>